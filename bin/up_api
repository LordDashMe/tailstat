#!/bin/bash

echo "Starting tailstat API..."
gunicorn --chdir /var/tailstat/src api:app --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000 &
echo "Successfully started tailstat API."
